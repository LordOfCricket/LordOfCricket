apiVersion: v1
kind: Service
metadata:
  name: loc-frontend
  namespace: loc
spec:
  # Phase 7 — see loc-backend-service.yaml's comment: same fix, same reason.
  type: ClusterIP

  selector:
    app: loc-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80