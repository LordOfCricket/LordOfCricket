# Phase 8 — same reasoning as loc-backend-pdb.yaml.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: loc-frontend-pdb
  namespace: loc
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: loc-frontend
